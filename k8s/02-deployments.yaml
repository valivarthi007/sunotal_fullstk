apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-frontend
  namespace: sunotal
  labels:
    app: sunotal-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-frontend
  template:
    metadata:
      labels:
        app: sunotal-frontend
    spec:
      containers:
        - name: frontend
          image: sunotal-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-auth
  namespace: sunotal
  labels:
    app: sunotal-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-auth
  template:
    metadata:
      labels:
        app: sunotal-auth
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: auth
          image: sunotal-auth:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
          env:
            - name: PORT
              value: "5001"
          envFrom:
            - configMapRef:
                name: sunotal-config
            - secretRef:
                name: sunotal-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 5001
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 5001
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 8
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-operations
  namespace: sunotal
  labels:
    app: sunotal-operations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-operations
  template:
    metadata:
      labels:
        app: sunotal-operations
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5002"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: operations
          image: sunotal-operations:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5002
          env:
            - name: PORT
              value: "5002"
          envFrom:
            - configMapRef:
                name: sunotal-config
            - secretRef:
                name: sunotal-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 5002
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 5002
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 8
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-inventory
  namespace: sunotal
  labels:
    app: sunotal-inventory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-inventory
  template:
    metadata:
      labels:
        app: sunotal-inventory
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: inventory
          image: sunotal-inventory:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5003
          env:
            - name: PORT
              value: "5003"
          envFrom:
            - configMapRef:
                name: sunotal-config
            - secretRef:
                name: sunotal-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 5003
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 5003
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 8
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-user
  namespace: sunotal
  labels:
    app: sunotal-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-user
  template:
    metadata:
      labels:
        app: sunotal-user
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5004"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: user
          image: sunotal-user:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5004
          env:
            - name: PORT
              value: "5004"
          envFrom:
            - configMapRef:
                name: sunotal-config
            - secretRef:
                name: sunotal-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 5004
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 5004
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 8
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sunotal-delivery
  namespace: sunotal
  labels:
    app: sunotal-delivery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sunotal-delivery
  template:
    metadata:
      labels:
        app: sunotal-delivery
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5006"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: delivery
          image: sunotal-delivery:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5006
          env:
            - name: PORT
              value: "5006"
          envFrom:
            - configMapRef:
                name: sunotal-config
            - secretRef:
                name: sunotal-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: 5006
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: 5006
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 8
