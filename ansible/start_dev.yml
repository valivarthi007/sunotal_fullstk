---
- name: "Launch Local Microservices Development Environment"
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Launch local Docker Compose microservices cluster
      command: docker compose up -d --build
      register: compose_res

    - name: Display cluster startup logs
      debug:
        var: compose_res.stdout_lines
